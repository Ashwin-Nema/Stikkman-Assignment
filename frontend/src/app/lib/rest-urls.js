export const hostname = 'http://localhost:4000/v1/';

export const backendUrls = {
  getAllCourses: `${hostname}courses`,
  getCourseById: `${hostname}course/`,
  deleteCourseById: `${hostname}course/`,
};
